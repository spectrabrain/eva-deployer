- import_playbook: ../../../site_infra.yaml
  vars:
    eva_repo_root: "{{ playbook_dir }}/../../.."
    eva_versions_file: "{{ playbook_dir }}/../../../versions.json"
    eva_install_root: "{{ playbook_dir }}/../../../install"
    eva_generated_config_root: "{{ playbook_dir }}/../../../config"
    eva_deploy_root: "{{ playbook_dir }}/../../../deploy"